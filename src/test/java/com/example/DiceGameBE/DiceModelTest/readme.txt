

//TODO Zadanie dla Tomasza:
/*Historyjka

Jako gracz chciałbym móc rzucić 5 kośćmi, kości powinny wyrzucać losowo wartości od 1-6,
 1 i 5 oraz wielokrotność (więcej niż 2 kości tej samej wartości) powinny mieć atrybut pozwalający rzucać dalej.

 Zadania:
    1. Stworzyć endpoint oraz serwis który będzie odpowiadał za rzut kośćmi
    1. Zgodnie z założeniami pierwszy rzut odbywa się 5 kostkami, każda z kości może wylosować liczby od 1 - 6
    2. Liczby 1 i 5 mają mieć status „GOOD_NUMBER”
    3. Liczby potrójne uzyskują status „GOOD_NUMBER” + „MULTIPLE”
    4. Na razie bez liczenia punktów, należy przekazać tylko wartości kości i ich atrybuty
    5. Atrybuty:
        1. Value (integer)
        2. isGoodNumber(boolean)
        3. isChecked(boolean)
        4. isMultiple(boolean)
        5. isImmutable(boolean)
 */
